import { FlowEnvironmentContent } from "../types";
import { baseSignatures } from "./signatures";
import { baseTypes } from "./types";

export const baseEnvironmentContent: FlowEnvironmentContent = {
    signatures: baseSignatures,
    types: baseTypes,
}