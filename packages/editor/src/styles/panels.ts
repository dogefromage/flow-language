import styled from "styled-components";

export const PanelHeaderDiv = styled.div`
    height: 40px;
    width: 100%;
    outline: 1px solid var(--color-2);
    padding: 0.5rem 1rem;
    display: flex;
    gap: 1rem;
`;

export const PanelHeadingH = styled.h3`
    height: 100%;
    text-transform: uppercase;
    transform: translateY(-1px); // font align
`;
